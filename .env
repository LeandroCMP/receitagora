# Configure your OpenAI credentials for recipe generation.
# Replace the placeholder value below with a valid API key.
OPENAI_API_KEY=
# Optionally override the API base URL or model.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
