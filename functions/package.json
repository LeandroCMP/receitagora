{
  "name": "receitagora-functions",
  "version": "1.0.0",
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "deploy": "firebase deploy --only functions",
    "lint": "echo 'No lint configured'"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.1",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.3"
  }
}
